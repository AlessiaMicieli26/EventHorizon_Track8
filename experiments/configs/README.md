# Experiment Configurations

Store your `.yaml` or `.json` files here to manage experiment parameters (hyperparameters, paths).

Current config:

- `adni_cyclegan_pipeline.json`: paths, split locations, training hyperparameters, and output directories for the executed ADNI CycleGAN pipeline.
